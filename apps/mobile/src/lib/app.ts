/** App-wide constants. APPNAME is a placeholder name (CLAUDE.md); keep it find-and-replaceable. */
export const APP_NAME = 'APPNAME';

/** Placeholder support address until the final domain exists (SPEC.md 11). */
export const SUPPORT_EMAIL = 'support@example.com';
