import React from 'react';

import { RoutePlaceholder } from '@/features/navigation/ui/RoutePlaceholder';

/** Joinable communities. A placeholder until prompt 4 builds them. */
export default function CommunitiesRoute() {
  return (
    <RoutePlaceholder
      icon="i-flag"
      title="Communities are coming"
      body="One for every team in all four leagues, each with its own leaderboards."
    />
  );
}
