import React from 'react';

import { RoutePlaceholder } from '@/features/navigation/ui/RoutePlaceholder';

/** Badges. A placeholder until prompt 4 builds them on the goals evaluator. */
export default function BadgesRoute() {
  return (
    <RoutePlaceholder
      icon="i-verified"
      title="Badges are coming"
      body="Earned for the games you go to, with the locked ones showing what it takes."
    />
  );
}
